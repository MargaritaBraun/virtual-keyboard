import { pull, margoName, runMargo } from "./module";
// console.log("Hello World!"); //eslint-disable-line no-console
runMargo();
// const nameI = "Margo Braun";

const nameArray = ["Ffff", "Afgf", "Ldds"];
for (let i = 0; i < nameArray.length; i++) {
  console.log(`Привет ${nameArray[i]}`);
}
